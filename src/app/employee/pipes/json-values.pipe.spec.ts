import { JsonValuesPipe } from './json-values.pipe';

describe('JsonValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
